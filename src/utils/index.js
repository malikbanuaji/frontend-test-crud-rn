import * as PopUp from './popUp';
import * as Validator from './validator';
import * as SmallMessage from './smallMessage';
import Permissions from './permissions';
import UImagePicker from './imagePicker';

const Utils = {
  PopUp,
  Validator,
  SmallMessage,
  Permissions,
  UImagePicker,
};

export default Utils;
